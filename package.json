{
  "name": "parse-midi",
  "version": "1.0.5",
  "description": "A small parser for MIDI messages.",
  "main": "dist/parseMidi.js",
  "scripts": {
    "start": "npm run clean && npm run build:demo -- -w",
    "build": "npm run clean && npm run build:es6 && npm run build:demo",
    "build:es6": "tsc",
    "build:demo": "tsc --module system --outFile dist/demoBundle.js",
    "clean": "rimraf dist",
    "lint": "tslint --format stylish --project .",
    "test": "jest",
    "prepublish": "npm test && npm run lint && npm run build"
  },
  "author": "Daniel Levett",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/dlevs/parse-midi"
  },
  "devDependencies": {
    "@dlevs/tslint-config": "1.0.4",
    "@types/jest": "23.3.1",
    "jest": "23.5.0",
    "rimraf": "2.6.2",
    "ts-jest": "23.1.4",
    "tslint": "5.11.0",
    "typescript": "3.0.3"
  }
}
